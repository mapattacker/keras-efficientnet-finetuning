# keras-efficientnet-finetuning

Example scripts for transfer learning of EfficientNet with Keras

* `project/config`: set training & inference parameters
* `project/train.py`: fine-tuning of model 
* `project/inference.py`: 